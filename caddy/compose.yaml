# Docker Compose for Caddy Webserver

services:
  caddy:
    image: caddy:latest
    container_name: caddy
    restart: always
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - ./.data:/data
      - ./.config:/config
    networks:
      - caddynet
networks:
  caddynet:
    name: caddynet
    driver: bridge
